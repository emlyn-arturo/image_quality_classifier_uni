# -*- coding: utf-8 -*-

from setuptools import setup, find_packages


setup(name='UnikittyPy',
      packages=find_packages(),
      install_requires=[
          # Install requirements from requirements.txt!
      ])
